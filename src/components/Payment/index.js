import React from 'react';
import { Layout } from './styled';
import UserCard from './UserCard';

const Payment = () => {
  return (
    <Layout>
      <UserCard />
    </Layout>
  );
};

export default Payment;
