import React from 'react';
import PrimaryButton from '../../../PrimaryButton';

const ReactionUpload = (props) => {
  return (
    <PrimaryButton className='action-btn'>Upload Reaction</PrimaryButton>
  )
}

export default ReactionUpload;
