import { connect } from 'react-redux';

import StarsignUp from './StarsignUp.component';

const mapStateToProps = state => ({
});

const mapDispatchToProps = dispatch => ({
});


export default connect(mapStateToProps, mapDispatchToProps)(StarsignUp);
