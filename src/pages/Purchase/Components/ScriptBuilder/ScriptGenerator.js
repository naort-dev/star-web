export const ScriptGenerator = ({
  category,
  for_name,
  from_name,
  relationship,
  date,
  occation,
  selfElse,
}) => {
  if (category === 'shout-out') {
      
  } else if (category === 'announce') {
  }
};
